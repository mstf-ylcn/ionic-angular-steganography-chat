import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ma.steganography.chat',
  appName: 'steganography-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
